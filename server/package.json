{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build":"npm install --legacy-peer-deps && npm install ajv@latest ajv-keywords@latest client && npm install --legacy-peer-deps --prefix client && npm run build --prefix client"
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.19.2",
    "http": "^0.0.1-security",
    "mongoose": "^6.1.6",
    "nodemon": "^2.0.22",
    "socket.io": "^4.4.1"
  }
}
